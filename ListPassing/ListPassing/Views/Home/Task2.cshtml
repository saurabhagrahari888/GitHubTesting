@model  IEnumerable<ListPassing.Models.person>
@{
    ViewBag.Title = "Task2";
    int i = 0;
}

<h2>Task2</h2>
@using (Html.BeginForm("Task2", "Home", FormMethod.Post))
{
<table border="1" cellpadding="6">
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.TextBox("[" + i + "].FName",item.FName)

            </td>
            <td>
                @Html.TextBox("[" + i + "].MName",item.MName)
                @Html.Hidden("[" + i + "].ID", item.ID)
            </td>
            <td>
                @Html.TextBox("[" + i + "].LName", item.LName)
            </td>
        </tr>
        i++;

    }

    <tr>
        <td colspan="4">
            <input type="submit" value="Submit" />
        </td>
    </tr>
</table>
    @*<table border="1" cellpadding="6">
                @for (int i = 0; i < Model.Count(); i++)
                {
                    <tr>
                        <td>
                            @Html.TextBox("FName[" + @i + "].FName",
                                      Model[i].)
                    </td>
                    <td>
                        @Html.TextBox("MName[" + @i + "].MName",
                                  Model[i].MName)
                </td>
                <td>
                    @Html.TextBox("LName[" + @i + "].LName",
                              Model[i].LName)
            </td>
        </tr>
        }
                <tr>
                    <td colspan="4">
                        <input type="submit" value="Submit" />
                    </td>
                </tr>
            </table>*@
        }
